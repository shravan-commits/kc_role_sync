[project]
name = "kc_role_sync"
authors = [
    { name = "Kalyan", email = "shravan.a.sahi.ext@kalyanj.onmicrosoft.com" }
]
description = "Auto-assign Keycloak roles to Frappe users on first SSO login"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
